This Project can work only **linux debian base**

**Insatllation**

```bash
# Follow this command
<<<<<<< HEAD

./install.sh
docker compose up -d --build
ufw allow 3000/tcp
cd src
=======
./install.sh
docker compose up -d --build
>>>>>>> a2ee187 (make file read env to config)
./run.sh
```

**Detect Able**

1. SSH Brute Force

2. FTP Brute Force

3. Port Scan (Syn Scan, Null Scan, XMAS Scan, Full XMAS Scan)

4. DoS/DDoS (Syn Flood, ICMP Flood, UDP Flood)

5. Path Traversal (../../)

6. SQL Injection

7. XSS

8. Web Brute Force (HTTP Brute Force)